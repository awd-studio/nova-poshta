language: php

php:
  - 7.0
  - 7.1

sudo: false

before_script:
  - curl --version
  - composer self-update
  - composer install --prefer-source --no-interaction --dev

script: vendor/bin/phpunit

matrix:
  fast_finish: true

notifications:
    on_success: never
    on_failure: always
